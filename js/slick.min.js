function initSlick() {
    $('.tarefas').slick({
      dots: true,
      infinite: true,
      slidesToShow: 1,
      adaptiveHeight: true,
      responsive: [

        {
          breakpoint: 1020, // Tamanho da tela em pixel
          settings: {
            slidesToShow: 1 // Quantidade de slides a serem exibidos quando a tela for igual ou menor que 702px
          }
        },
        {
          breakpoint: 8000, // Tamanho da tela em pixel
          settings: 'unslick'
        }
        
      ]
    });
  }
  
  // Função para reconfigurar o Slick quando ocorrer uma alteração no tamanho da janela
  function handleWindowResize() {
    // Destruir o Slick existente
    $('.tarefas').slick('unslick');
    // Inicializar novamente o Slick
    initSlick();
  }

  function inicializa () {
    initSlick();
  
    
  }
  
 
  window.addEventListener('DOMContentLoaded', inicializa);
  window.addEventListener('resize', handleWindowResize);

  

  